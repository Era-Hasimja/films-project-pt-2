import { User } from "../../api/users/Api";

interface Props {
  data: User[];
}
export const UsersTable = () => {
  return (
    <div>
      {/* <DataGrid
        rows={rows}
        columns={columns}
        pageSize={5}
        rowsPerPageOptions={[5]}
        checkboxSelection
      /> */}
    </div>
  );
};
